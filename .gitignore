node_modules/
.DS_Store
bun.lock

# Channel state (credentials, access control, inbox files)
# These are runtime state, not part of the plugin code
.env
*.env.local
access.json
approved/
inbox/

# Claude Code project-specific files
.claude/settings.json
.claude/project.json
.claude/tasks/

# Runtime debug logs and temp plugin clones (not source)
.claude/log/[0-9]*-feishu/
.claude/tmp-feishu-plugin/
