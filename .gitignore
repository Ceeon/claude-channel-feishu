node_modules/
.DS_Store
bun.lock

# Channel state (credentials, access control, inbox files)
# These are runtime state, not part of the plugin code
.env
*.env.local
access.json
approved/
inbox/

# Claude Code agent workspace — author's internal notes, not part of the plugin
.claude/

# Internal dev/investigation logs
/log/
